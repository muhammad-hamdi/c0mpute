{
  "name": "c0mpute",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*",
    "plugins/*/web"
  ],
  "scripts": {
    "dev:web": "bun run --cwd plugins/transcode/web dev",
    "dev:coordinator": "bun run --cwd apps/coordinator dev",
    "build": "bun run --cwd packages/shared build && bun run --cwd plugins/transcode/web build",
    "lint": "bun run --cwd apps/coordinator lint",
    "cargo:build": "cargo build --bins",
    "cargo:run": "cargo run --bin c0mpute --",
    "cargo:test": "cargo test --workspace"
  },
  "devDependencies": {
    "typescript": "^5.8.0"
  }
}
